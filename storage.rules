rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /documents/{fileName} {
      allow read: if request.auth != null;
      allow write: if request.auth != null; // Further restricted server-side
    }
  }
}